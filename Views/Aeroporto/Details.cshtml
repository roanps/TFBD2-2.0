@model VoeMais.Models.Aeroporto
@{
    ViewData["Title"] = "Detalhes Aeroporto";
}
<h2>@ViewData["Title"]</h2>

<dl class="row">
    <dt class="col-sm-2">ID</dt>
    <dd class="col-sm-10">@Model.IdAeroporto</dd>

    <dt class="col-sm-2">Nome</dt>
    <dd class="col-sm-10">@Model.Nome</dd>

    <dt class="col-sm-2">Cidade</dt>
    <dd class="col-sm-10">@Model.Cidade</dd>

    <dt class="col-sm-2">Estado</dt>
    <dd class="col-sm-10">@Model.Estado</dd>

    <dt class="col-sm-2">Código IATA</dt>
    <dd class="col-sm-10">@Model.CodigoIATA</dd>


</dl>

<a asp-action="Index" class="btn btn-secondary">Voltar</a>
