@model VoeMais.Models.Escala
@{
    ViewData["Title"] = "Excluir Escala";
}
<h2>@ViewData["Title"]</h2>

<p>Tem certeza que deseja excluir este registro?</p>

<dl class="row">
    <dt class="col-sm-2">ID</dt>
    <dd class="col-sm-10">@Model.IdEscala</dd>

    <dt class="col-sm-2">Aeroporto Escala</dt>
    <dd class="col-sm-10">@Model.IdAeroportoEscala</dd>

    <dt class="col-sm-2">Chegada</dt>
    <dd class="col-sm-10">@Model.ChegadaEscala</dd>

    <dt class="col-sm-2">Saída</dt>
    <dd class="col-sm-10">@Model.SaidaEscala</dd>


</dl>

<form asp-action="Delete" method="post">
    <button type="submit" class="btn btn-danger">Excluir</button>
    <a asp-action="Index" class="btn btn-secondary">Cancelar</a>
</form>
